# esp32 软件设计
## 1.功能描述
- 配置功能
- 串口功能
- TCP、UDP功能
- web配置
## 2.软件设计
### 2.1 配置功能
配置参数：
- 网络模式
- 本地IP，端口
- 目标1IP，端口
- 目标2IP，端口
- DHCP标志
- HTTP标志

### 2.2 串口功能


### 2.3 TCP、UDP功能
具备TCP透传、UDP透传、UDP固定端口配置

首先读取配置
```c
{
    uint8_t workingmode ;
    char ssid[SYS_SSID_MAX];
    char password[SYS_PASSWORD_MAX];
    uint8_t dhcp_flag;
    uint32_t DNS;
    uint32_t gateway;
    uint8_t domainname_flag;
    char domain_name[SYS_DOMAIN_NAME_MAX];
    uint32_t target_ip;
    uint16_t target_port;
    uint32_t local_ip;
    uint16_t local_port;
    uint16_t local_conf_port;
}
```
TCP、UDP
网络复位，重新开始
触发连接AP
连接完成判断模式

### 2.4 web配置
完成HTTP的参数配置功能



